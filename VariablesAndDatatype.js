// #2

// Variable

    // let age = 25;  age -> name , 25 -> value;

    // Variable is named memory location

    // var x = 10;
    // var x = 30;
    // (It is possible)

// --------------------------------------------------

// let,const

    // (It is Not possible)
    // let x = 10;  // const x = 10;  // const x = 230;
    // let x = 30;  // const x = 30;  // x = 24;

    //(It is possible) Dynamic Typed Language
    // let x = 10;
    // x = "pyaar";
    // x = true;
    // x = null;

// ---> Summary of all these is that generally we are using let,const not var

// --------------------------------------------------

// Data Types

    // Data type define the type of data and size of data

    // 1. Primitive DataType -------------------> Number , String , Boolean , Undefined , Null , BigInt , Symbol
    // 2. Non - Primitive DataType

    // Primitive DataType

    // Number = 2,4,3,4.4,356,
    // String = "pyarr" , "Namaste" , "Chai"
    // Boolean = true / false
    // undefined = not defined
    // Null = let value = null;
    // BigInt = 2^53-1
    // Symbol ----------------------->Symbol is a built-in object whose constructor returns a symbol primitive — also called a Symbol value or just a Symbol — that's guaranteed to be unique.

// --------------------------------------------------------->