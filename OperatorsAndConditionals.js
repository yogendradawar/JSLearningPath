// #3

// Operators

    // operations ----> work ---> Symbols

    // Types of Operators

        // 1. Arithmetic ------------> Binary a+b,a-c ,  Uniary ++a,--a  ----> pre,post
        
            // +,-,*,/,%,**

            // let a = 2; 
            // let b = 3;

            // console.log(a**b)         ** ----------> Power Operator

        
        // 2. Assignment Operators 

            // =, += , -= , /= , %= , *= 

        
        // 3. Comparison Operators

            // >,<,>=,<=,   ==,===,!=,!==


            // Stric Equality (===) (!==)v/s  Loose Equality (==) (!=)

                // (!==) === ----------> Same Value + typeofdata
                // (!=)  == -----------> Same Value + (typeofdata same nhi hai to chal jayega)


        // 4. Ternary Operator

            // condition ? val1 : val2 ;

            // let age = 24;
            // let status = (age > 18) ? 'I can vote' : ' I cannot vote';

            // console.log(status);

        
        // 5. Logical Operators

            // &&, || , ! 

            // Working with Non - Boolean

                // (false && "pyarr");
                // (false || "zer");
                // (true || 'Hello yaar')

            // ---------> (To understand these NON - BOOLEAN operator these falscy , Truthy are apply)

            // Falsy --> undefined , null, 0 ,false , Nan , ' ' 
            // Truly --> Anything which is not considered Falsy

            // ----------> Short-Circuiting

                // (false || false || true || false) 

            
        // 6. Bitwise Operators

                // & , | , ~ , << , >> , ^ 

                    // ~ -> Not  , ^ -> XOR , << ---leftShift >> ----RightShift 

                    // leftShift -----> number * (2 ^ shiftValue)
                    // rightShift -----> number / (2 ^ shiftValue)


// --------------------------------------------------------------------------------------------


// Conditionals :-

    // if-else, switch case

    // Template literals are literals delimited with backtick (`) 